# Google Service Account (JSON key content as single line, or path to file)
# Option A: Path to service account JSON file (local dev)
GOOGLE_APPLICATION_CREDENTIALS=./service-account.json

# Option B: For Vercel - paste the entire JSON as one line in GOOGLE_SERVICE_ACCOUNT_JSON
# GOOGLE_SERVICE_ACCOUNT_JSON={"type":"service_account","project_id":"..."}

# Google Sheets ID (from the URL: https://docs.google.com/spreadsheets/d/YOUR_SHEET_ID/edit)
GOOGLE_SHEET_ID=your_sheet_id_here

# Google Drive folder ID for storing cover images (create a folder, copy ID from URL)
# NOTE: Service Account + @gmail.com มักอัปโหลดขึ้น My Drive ไม่ได้ (ไม่มี storage quota)
GOOGLE_DRIVE_FOLDER_ID=your_folder_id_here

# Cloudinary (แนะนำสำหรับ @gmail.com + deploy บน Vercel)
# สมัครแล้วเอาค่าจาก Dashboard ใส่ให้ครบ
CLOUDINARY_CLOUD_NAME=your_cloud_name
CLOUDINARY_API_KEY=your_api_key
CLOUDINARY_API_SECRET=your_api_secret
